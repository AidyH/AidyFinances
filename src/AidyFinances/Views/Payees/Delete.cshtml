@model AF.ViewModels.Payees.PayeeViewModel

<h1>Delete Payee</h1>

<div class="ui segment">    
    <div class="ui form">
        <form asp-controller="Payees" asp-action="Delete" method="post" class="ui large form validate-me" role="form">
            <div class="field">
                <p>Are you sure you want to delete @Model.Name?
            </div>
            <div class="field">
                <input type="submit" value="Delete" class="red ui button" />
                <a asp-controller="Payees" asp-action="Index">Cancel</a>
                <input type="hidden" asp-for="PayeeId" />
            </div>
        </form>
    </div>
</div>